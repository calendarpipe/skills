# Copy to .env and fill in. .env is gitignored.
# Only needed for integration tests — unit tests use a fake token.

# Required. The integration test creates a throwaway hosted calendar,
# exercises it, and deletes it afterward. No other setup needed.
CALENDARPIPE_API_TOKEN=
